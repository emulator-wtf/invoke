{
  "name": "invoke",
  "version": "0.0.2",
  "description": "Github action to invoke instrumented Android tests with emulator.wtf",
  "scripts": {
    "build": "rimraf dist && tsc"
  },
  "homepage": "https://github.com/emulator-wtf/invoke",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^16.18.24",
    "typescript": "^5.0.4",
    "rimraf": "^5.0.0"
  }
}
